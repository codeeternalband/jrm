{
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html,*.css,*.js,*.png,*.jpg,*.svg\" --host 127.0.0.1 --port 8080 --no-open"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
